import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1
  },
  title: {
    fontSize: 32,
    textAlign: 'center',
    color: 'black',
    marginTop: 5
  }
});
